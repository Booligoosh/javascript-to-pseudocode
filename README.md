# JavaScript to pseudo-code converter (work in progress)
To run the code, which converts the JavaScript file `program.js`, install the dependencies (`npm install`) then type `npm start` into your terminal.